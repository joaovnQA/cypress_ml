name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Verify requirements.txt exists
        run: |
          ls -la

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.8

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install numpy==1.21.6
          pip install joblib==1.1.0

      - name: Install Node.js and npm Dependencies
        uses: actions/setup-node@v3
        with:
          node-version: "16"

      - name: Start the App
        run: |
          nohup python app.py &  # Inicia a aplicação Flask em segundo plano
        env:
          FLASK_ENV: development

      - name: Wait for the App
        run: |
          echo "Esperando o servidor Flask iniciar..."
          for i in {1..20}; do
            if curl -s http://127.0.0.1:5000 > /dev/null; then
              echo "App is ready!"
              break
            fi
            echo "Waiting for the app to start... (tentativa $i)"
            sleep 5
          done

      - name: Install npm Dependencies
        run: |
          npm install
        working-directory: ./tests/e2e

      - name: Install Cypress
        uses: cypress-io/github-action@v4
        with:
          working-directory: ./tests/e2e

      - name: Run Cypress Tests
        run: npx cypress run
        env:
          CYPRESS_BASE_URL: http://127.0.0.1:5000
        working-directory: ./tests/e2e

      - name: Validate Inference Quality
        run: python scripts/validate_inference.py
        env:
          MODEL_ENDPOINT: http://127.0.0.1:5000
